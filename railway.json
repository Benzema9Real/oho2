{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd ohO && /app/venv/bin/python manage.py migrate && /app/venv/bin/python create_superuser.py && /app/venv/bin/gunicorn oho_kg.wsgi:application --bind 0.0.0.0:$PORT --workers 2",
    "restartPolicyType": "ON_FAILURE"
  }
}