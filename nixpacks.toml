[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["cd ohO && pip install -r requirements.txt --break-system-packages"]

[phases.build]
cmds = ["cd ohO && python manage.py collectstatic --noinput"]

[start]
cmd = "cd ohO && gunicorn oho_kg.wsgi"